\documentclass{article}

\usepackage{tikz} 
\usetikzlibrary{automata, positioning, arrows} 

\usepackage{amsthm}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{fullpage}
\usepackage{color}
\usepackage{parskip}
\usepackage{float}
\usepackage{hyperref}
  \hypersetup{
    colorlinks = true,
    urlcolor = blue,       % color of external links using \href
    linkcolor= blue,       % color of internal links 
    citecolor= blue,       % color of links to bibliography
    filecolor= blue,        % color of file links
    }
    
\usepackage{graphicx}
    
\usepackage{listings}
\usepackage[utf8]{inputenc}                                                    
\usepackage[T1]{fontenc}                                                       

\definecolor{dkgreen}{rgb}{0,0.6,0}
\definecolor{gray}{rgb}{0.5,0.5,0.5}
\definecolor{mauve}{rgb}{0.58,0,0.82}

\lstset{frame=tb,
  language=haskell,
  aboveskip=3mm,
  belowskip=3mm,
  showstringspaces=false,
  columns=flexible,
  basicstyle={\small\ttfamily},
  numbers=none,
  numberstyle=\tiny\color{gray},
  keywordstyle=\color{blue},
  commentstyle=\color{dkgreen},
  stringstyle=\color{mauve},
  breaklines=true,
  breakatwhitespace=true,
  tabsize=3
}

\newtheoremstyle{theorem}
  {\topsep}   % ABOVESPACE
  {\topsep}   % BELOWSPACE
  {\itshape\/}  % BODYFONT
  {0pt}       % INDENT (empty value is the same as 0pt)
  {\bfseries} % HEADFONT
  {.}         % HEADPUNCT
  {5pt plus 1pt minus 1pt} % HEADSPACE
  {}          % CUSTOM-HEAD-SPEC
\theoremstyle{theorem} 
   \newtheorem{theorem}{Theorem}[section]
   \newtheorem{corollary}[theorem]{Corollary}
   \newtheorem{lemma}[theorem]{Lemma}
   \newtheorem{proposition}[theorem]{Proposition}
\theoremstyle{definition}
   \newtheorem{definition}[theorem]{Definition}
   \newtheorem{example}[theorem]{Example}
\theoremstyle{remark}    
  \newtheorem{remark}[theorem]{Remark}

\title{CPSC-406 Report}
\author{Andrew Floyd  \\ Chapman University}

\date{\today} 

\begin{document}

\maketitle

\begin{abstract}
\end{abstract}

\setcounter{tocdepth}{3}
\tableofcontents

\section{Introduction}\label{intro}

\section{Week by Week}\label{homework}

\subsection{Week 1}

\subsubsection{Exercise 1: Word Processing with DFAs}

\textbf{Notes and Homework:}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.8\textwidth]{../BkD7CQiYJe.jpg}
  \caption{Automata $A_1$ and $A_2$}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.8\textwidth]{../Bk0mRmjYJg.jpg}
  \caption{Additional automata reference}
\end{figure}

\textbf{Question 1:} Which of the following words are accepted/refused by $A_1$ and $A_2$, respectively? Complete the table.

\begin{table}[H]
  \centering
  \renewcommand{\arraystretch}{1.2}
  \begin{tabular}{|c|c|c|}
    \hline
    $w$ & accepted by $A_1$? & accepted by $A_2$? \\ \hline
    $aaa$ & no & yes \\ \hline
    $aab$ & yes & no \\ \hline
    $aba$ & no & no \\ \hline
    $abb$ & no & no \\ \hline
    $baa$ & no & yes \\ \hline
    $bab$ & no & no \\ \hline
    $bba$ & no & no \\ \hline
    $bbb$ & no & no \\ \hline
  \end{tabular}
  \caption{Acceptance table for $A_1$ and $A_2$}
\end{table}

\textbf{Question 2:} More generally, can you completely describe the languages $L(A_k)$ accepted by $A_k$, for $k = 1, 2$?

The language $L(A)$ accepted by this DFA is:
$$L(A) = \{ w \in \{a,b\}^* \mid \text{the number of } a \text{'s in } w \text{ is even} \}$$

\subsubsection{Exercise 2: Designing DFAs}

Design DFAs whose accepted languages are given as follows:

\begin{enumerate}
  \item All the words that end with $ab$
  \item All the words that contain $ab$
  \item All the words that contain an odd number of $a$'s and an odd number of $b$'s
  \item All the words that contain an even number of $a$'s and an odd number of $b$'s
  \item All the words such that any three consecutive characters contain at least one $a$
  \item All the words that contain $aba$
\end{enumerate}

\textbf{DFA Diagrams:}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=3cm, ->, >=stealth]
    \node[state, initial] (q0) {$q_0$};
    \node[state] (q1) [right of=q0] {$q_1$};
    \node[state, accepting] (q2) [right of=q1] {$q_2$};
    
    \path (q0) edge [loop above] node {$b$} (q0);
    \path (q0) edge [above] node {$a$} (q1);
    
    \path (q1) edge [loop above] node {$a$} (q1);
    \path (q1) edge [above] node {$b$} (q2);
    
    \path (q2) edge [bend left=30, below] node {$a$} (q1);
    \path (q2) edge [bend right=50, above] node {$b$} (q0);
  \end{tikzpicture}
  \caption{DFA for words ending with $ab$}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.5cm, ->, >=stealth]
    \node[state, initial] (q0) {$q_0$};
    \node[state] (q1) [right of=q0] {$q_1$};
    \node[state] (q2) [right of=q1] {$q_2$};
    \node[state, accepting] (q3) [right of=q2] {$q_3$};
    
    \path (q0) edge [loop above] node {$b$} (q0);
    \path (q0) edge [above] node {$a$} (q1);
    
    \path (q1) edge [bend left=20, above] node {$a$} (q0);
    \path (q1) edge [above] node {$b$} (q2);
    
    \path (q2) edge [above] node {$a$} (q3);
    \path (q2) edge [bend right=50, below] node {$b$} (q0);
    
    \path (q3) edge [loop above] node {$a, b$} (q3);
  \end{tikzpicture}
  \caption{DFA for words containing $aba$}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=3cm, ->, >=stealth]
    % States: (even a, even b), (odd a, even b), (even a, odd b), (odd a, odd b)
    \node[state, initial] (q00) {$q_{00}$};
    \node[state] (q10) [right of=q00] {$q_{10}$};
    \node[state] (q01) [below of=q00] {$q_{01}$};
    \node[state, accepting] (q11) [right of=q01] {$q_{11}$};
    
    % a transitions (toggle first index)
    \path (q00) edge [bend left, above] node {$a$} (q10);
    \path (q10) edge [bend left, below] node {$a$} (q00);
    \path (q01) edge [bend left, above] node {$a$} (q11);
    \path (q11) edge [bend left, below] node {$a$} (q01);
    
    % b transitions (toggle second index)
    \path (q00) edge [bend left, left] node {$b$} (q01);
    \path (q01) edge [bend left, right] node {$b$} (q00);
    \path (q10) edge [bend left, left] node {$b$} (q11);
    \path (q11) edge [bend left, right] node {$b$} (q10);
  \end{tikzpicture}
  \caption{DFA for words with odd number of $a$'s and odd number of $b$'s}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.5cm, ->, >=stealth]
    % States: q0=(even a, even b), q1=(odd a, even b), q2=(odd a, odd b), q3=(even a, odd b) ACCEPT
    \node[state, initial] (q0) {$q_0$};
    \node[state] (q1) [right of=q0] {$q_1$};
    \node[state] (q2) [right of=q1] {$q_2$};
    \node[state, accepting] (q3) [right of=q2] {$q_3$};
    
    % a transitions (q0 <-> q1, q3 <-> q2)
    \path (q0) edge [bend left=20, above] node {$a$} (q1);
    \path (q1) edge [bend left=20, below] node {$a$} (q0);
    \path (q3) edge [bend left=20, above] node {$a$} (q2);
    \path (q2) edge [bend left=20, below] node {$a$} (q3);
    
    % b transitions (q0 <-> q3, q1 <-> q2)
    \path (q0) edge [bend left=40, above] node {$b$} (q3);
    \path (q3) edge [bend left=40, below] node {$b$} (q0);
    \path (q1) edge [bend left=40, above] node {$b$} (q2);
    \path (q2) edge [bend left=40, below] node {$b$} (q1);
  \end{tikzpicture}
  \caption{DFA for words with even number of $a$'s and odd number of $b$'s}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.5cm, ->, >=stealth]
    % Top row: q0 (start), q1, q2, q3 (final)
    \node[state, initial] (q0) {$q_0$};
    \node[state] (q1) [right of=q0] {$q_1$};
    \node[state] (q2) [right of=q1] {$q_2$};
    \node[state, accepting] (q3) [right of=q2] {$q_3$};
    
    % Right column: q4 (final), q5 (final)
    \node[state, accepting] (q4) [below of=q3] {$q_4$};
    \node[state, accepting] (q5) [below of=q4] {$q_5$};
    
    % Bottom left: q6
    \node[state] (q6) [left of=q5, xshift=-2.5cm] {$q_6$};
    
    % Top row transitions (b's going right)
    \path (q0) edge [above] node {$b$} (q1);
    \path (q1) edge [above] node {$b$} (q2);
    \path (q2) edge [above] node {$a$} (q3);
    
    % Self-loops
    \path (q0) edge [loop above] node {$a$} (q0);
    \path (q3) edge [loop right] node {$a$} (q3);
    \path (q6) edge [loop left] node {$a$} (q6);
    
    % Long 'a' arc from q0 over the top to q3
    \path (q0) edge [bend left=50, above] node {$a$} (q3);
    
    % Vertical transitions on right (b going down)
    \path (q3) edge [right] node {$b$} (q4);
    \path (q4) edge [right] node {$b$} (q5);
    
    % a transitions between q3, q4
    \path (q4) edge [bend left=20, left] node {$a$} (q3);
    
    % Bottom transitions
    \path (q5) edge [above] node {$b$} (q6);
    \path (q6) edge [bend left=30, below] node {$b$} (q5);
  \end{tikzpicture}
  \caption{DFA for words where any three consecutive characters contain at least one $a$}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.5cm, ->, >=stealth]
    % States: q0 = seen 0 b's, q1 = seen 1 b, q2 = seen 2 b's, q3 = seen bbb (accept)
    \node[state, initial] (q0) {$q_0$};
    \node[state] (q1) [right of=q0] {$q_1$};
    \node[state] (q2) [right of=q1] {$q_2$};
    \node[state, accepting] (q3) [right of=q2] {$q_3$};
    
    % b transitions (moving right toward accepting state)
    \path (q0) edge [above] node {$b$} (q1);
    \path (q1) edge [above] node {$b$} (q2);
    \path (q2) edge [above] node {$b$} (q3);
    
    % a transitions (reset to q0)
    \path (q0) edge [loop above] node {$a$} (q0);
    \path (q1) edge [bend left=30, below] node {$a$} (q0);
    \path (q2) edge [bend left=40, below] node {$a$} (q0);
    
    % Once in q3, stay there on any input
    \path (q3) edge [loop above] node {$a$} (q3);
    \path (q3) edge [loop below] node {$b$} (q3);
  \end{tikzpicture}
  \caption{DFA for words containing $bbb$}
\end{figure}

\vspace{0.3cm}

\textbf{What do you notice when comparing the various automata?}

When comparing the DFAs designed above, several patterns emerge:

\begin{itemize}
  \item \textbf{State count reflects language complexity:} Simple properties like "ends with $ab$" require only 3 states, while tracking multiple independent conditions (like counting both $a$'s and $b$'s modulo 2) requires $2^n$ states where $n$ is the number of independent conditions being tracked.
  
  \item \textbf{Parity-based languages form grid structures:} DFAs for languages involving "odd/even" counts tend to have states arranged in a grid pattern, where each dimension corresponds to one parity condition. The transitions toggle between states along each axis.
  
  \item \textbf{Substring containment uses linear chains:} DFAs that check for substring containment (like "contains $ab$" or "contains $bbb$") have a linear chain of states tracking progress toward finding the substring, with "reset" transitions back to earlier states when the pattern is broken.
  
  \item \textbf{Accepting states indicate language structure:} For "contains" languages, once the target is found, we stay in an accepting sink state. For parity languages, specific combinations of parities determine acceptance.
  
  \item \textbf{Self-loops handle "don't care" inputs:} When an input symbol doesn't affect progress toward acceptance (like $a$'s in "contains $bbb$" after reaching the accepting state), self-loops keep the automaton in place.
\end{itemize}

\section{Synthesis}

\subsection{Week 2}

\subsubsection{Exercise 1: Product Automata}

\textbf{Homework:} Work with the two DFAs shown below, describe their languages, and build the product automata needed for intersection and union.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.5cm, ->, >=stealth]
    \node[state, initial] (a1) {$1$};
    \node[state, accepting] (a2) [right of=a1] {$2$};
    \node[state] (a3) [right of=a2] {$3$};
    \node[state, accepting] (a4) [below of=a2] {$4$};
    \path (a1) edge [above] node {$a$} (a2)
               edge [bend left, left] node {$b$} (a4)
          (a2) edge [above] node {$a$} (a3)
               edge [bend left, left] node {$b$} (a4)
          (a3) edge [loop above] node {$a,b$} (a3)
          (a4) edge [bend left, right] node {$a$} (a2)
               edge [bend left, below] node {$b$} (a3);
  \end{tikzpicture}
  \caption{Automaton $\mathcal{A}^{(1)}$}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.5cm, ->, >=stealth]
    \node[state, initial] (b1) {$1$};
    \node[state, accepting] (b2) [right of=b1] {$2$};
    \node[state] (b3) [below of=b1] {$3$};
    \path (b1) edge [above] node {$a$} (b2)
               edge [left] node {$b$} (b3)
          (b2) edge [bend left] node {$a,b$} (b1)
          (b3) edge [loop below] node {$a,b$} (b3);
  \end{tikzpicture}
  \caption{Automaton $\mathcal{A}^{(2)}$}
\end{figure}

\textbf{Question 1: Describe $L(\mathcal{A}^{(1)})$ and $L(\mathcal{A}^{(2)})$.}

\begin{itemize}
  \item $L(\mathcal{A}^{(1)})$ is “all strings that stop the moment you land in state 2 or jump from 2 to 4.” You must reach 2 by reading an $a$. You may then loop through “$a$ back to 2, $b$ down to 4” as often as you like, but the word has to end right after that last $a$ into 2 or $b$ into 4.
  \item $L(\mathcal{A}^{(2)})$ is “all strings that start with $a$, never fall into state 3, and stay parked in 2.” Once you enter 2 on the first $a$, every later letter must be whatever keeps you there; if you ever take the $b$ to 3 the word is rejected.
\end{itemize}

\textbf{Question 2: Construct the intersection automaton $\mathcal{A}$.}

The standard product construction uses ordered pairs $(p,q)$ where $p$ is a state of $\mathcal{A}^{(1)}$ and $q$ a state of $\mathcal{A}^{(2)}$. We only draw reachable states.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[->, >=stealth]
    \node[state, initial] (s11) at (0,0) {$(1,1)$};
    \node[state, accepting] (s22) at (3,0) {$(2,2)$};
    \node[state] (s31) at (6,0) {$(3,1)$};
    \node[state] (s32) at (9,0) {$(3,2)$};
    \node[state] (s41) at (3,-2) {$(4,1)$};
    \node[state] (s33) at (6,-2) {$(3,3)$};
    \node[state] (s43) at (0,-3) {$(4,3)$};
    \node[state] (s23) at (3,-3) {$(2,3)$};
    \path (s11) edge [above] node {$a$} (s22)
       edge [left] node {$b$} (s43)
     (s22) edge [above] node {$a$} (s31)
       edge [right] node {$b$} (s41)
     (s31) edge [above] node {$a$} (s32)
       edge [right] node {$b$} (s33)
     (s32) edge [bend left] node {$a,b$} (s31)
     (s41) edge [left] node {$a$} (s22)
       edge [below] node {$b$} (s33)
     (s43) edge [above] node {$a$} (s23)
       edge [below] node {$b$} (s33)
     (s23) edge [above] node {$a$} (s33)
       edge [above] node {$b$} (s43)
     (s33) edge [loop below] node {$a,b$} (s33);
  \end{tikzpicture}
  \caption{Intersection automaton $\mathcal{A}$}
\end{figure}

\textbf{Question 3: Show $L(\mathcal{A}) = L(\mathcal{A}^{(1)}) \cap L(\mathcal{A}^{(2)})$.}

The product DFA only has one double-circle state, $(2,2)$. Getting there means the $\mathcal{A}^{(1)}$ copy is sitting happily in 2 (after possibly bouncing through 4) while the $\mathcal{A}^{(2)}$ copy is also in 2. So a word survives in the product exactly when it makes  both  original machines happy at the same time, i.e., when it lies in the intersection.

\textbf{Question 4: Modify $\mathcal{A}$ to obtain $\mathcal{A}'$ with $L(\mathcal{A}') = L(\mathcal{A}^{(1)}) \cup L(\mathcal{A}^{(2)})$.}

Keep the same transition structure but change the accepting set to
\[
F' = \{ (p,q) \mid p \in F_{\mathcal{A}^{(1)}} \text{ or } q \in F_{\mathcal{A}^{(2)}} \}.
\]
Now any word accepted by either original automaton drives the product to a state whose pair has at least one accepting component, so the language is the union.

\subsubsection{Exercise 2: More Automata}

\textbf{Homework:} Repeat the same analysis for the DFAs $\mathcal{B}^{(1)}$ and $\mathcal{B}^{(2)}$ below.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.4cm, ->, >=stealth]
    \node[state, initial, accepting] (p0) {$p_0$};
    \node[state] (p1) [right of=p0] {$p_1$};
    \node[state] (p2) [right of=p1] {$p_2$};
    \path (p0) edge [loop above] node {$b$} (p0)
     edge [above] node {$a$} (p1)
     (p1) edge [loop above] node {$b$} (p1)
     edge [above] node {$a$} (p2)
     (p2) edge [loop above] node {$b$} (p2)
     edge [bend right=40, below] node {$a$} (p0);
  \end{tikzpicture}
  \caption{Automaton $\mathcal{B}^{(1)}$}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.4cm, ->, >=stealth]
    \node[state, initial, accepting] (q0) {$q_0$};
    \node[state] (q1) [right of=q0] {$q_1$};
    \node[state] (q2) [right of=q1] {$q_2$};
    \path (q0) edge [loop above] node {$b$} (q0)
     edge [above] node {$a$} (q1)
     (q1) edge [above] node {$a$} (q2)
     edge [bend right=40, below] node {$b$} (q0)
     (q2) edge [loop above] node {$a,b$} (q2);
  \end{tikzpicture}
  \caption{Automaton $\mathcal{B}^{(2)}$}
\end{figure}

\textbf{Question 1: Describe $L(\mathcal{B}^{(1)})$ and $L(\mathcal{B}^{(2)})$.}

\begin{itemize}
  \item $L(\mathcal{B}^{(1)})$ = “count $a$’s in groups of three and ignore $b$’s.” The machine walks through $p_0 \rightarrow p_1 \rightarrow p_2 \rightarrow p_0$ every time an $a$ shows up, so you only end in the accepting home state $p_0$ when the total number of $a$’s is a multiple of three.
  \item $L(\mathcal{B}^{(2)})$ = “never write two $a$’s back-to-back.” You can stay in $q_0$ on $b$’s, hop to $q_1$ on a single $a$, but a second immediate $a$ throws you into the sink $q_2$. As long as every $a$ is separated by at least one $b$, you return to $q_0$ and the word is accepted.
\end{itemize}

\textbf{Question 2: Construct the intersection automaton $\mathcal{B}$.}

The full product has nine reachable states. Starting from $(p_0,q_0)$ we obtain the transition graph shown below. Because only $p_0$ and $q_0$ are accepting in their respective machines, the sole accepting pair is $(p_0,q_0)$.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[->, >=stealth]
    \node[state, initial, accepting] (s00) at (0,0) {$(p_0,q_0)$};
    \node[state] (s10) at (3,0) {$(p_1,q_0)$};
    \node[state] (s20) at (6,0) {$(p_2,q_0)$};
    \node[state] (s01) at (0,-2.4) {$(p_0,q_1)$};
    \node[state] (s11) at (3,-2.4) {$(p_1,q_1)$};
    \node[state] (s21) at (6,-2.4) {$(p_2,q_1)$};
    \node[state] (s02) at (0,-4.8) {$(p_0,q_2)$};
    \node[state] (s12) at (3,-4.8) {$(p_1,q_2)$};
    \node[state] (s22) at (6,-4.8) {$(p_2,q_2)$};
    \path (s00) edge [bend left=10] node {$a$} (s11)
                 edge [loop left] node {$b$} (s00)
          (s10) edge [bend left=10] node {$a$} (s21)
                 edge [loop above] node {$b$} (s10)
          (s20) edge [bend left=10] node {$a$} (s01)
                 edge [loop above] node {$b$} (s20)
          (s01) edge [bend left=10] node {$a$} (s12)
                 edge [left] node {$b$} (s00)
          (s11) edge [bend left=10] node {$a$} (s22)
                 edge [left] node {$b$} (s10)
          (s21) edge [bend left=10] node {$a$} (s02)
                 edge [right] node {$b$} (s20)
          (s02) edge [bend left=10] node {$a$} (s12)
                 edge [loop below] node {$b$} (s02)
          (s12) edge [bend left=10] node {$a$} (s22)
                 edge [loop below] node {$b$} (s12)
          (s22) edge [bend left=10] node {$a$} (s02)
                 edge [loop below] node {$b$} (s22);
  \end{tikzpicture}
  \caption{Intersection automaton $\mathcal{B}$}
\end{figure}

\textbf{Question 3: Why does $L(\mathcal{B}) = L(\mathcal{B}^{(1)}) \cap L(\mathcal{B}^{(2)})$?}

Each arrow in the product just mirrors the arrows in the two base DFAs. The only double-circle state is $(p_0,q_0)$, so the combined machine says “yes” exactly when both halves also say “yes” at the end of the word. That is the definition of the intersection.

\textbf{Question 4: Obtaining the union language.}

As in Exercise 1, we can apply De Morgan's law. Complement each DFA by flipping accepting/non-accepting states, build the product, and complement again. Equivalently, start from the product above and simply mark as accepting every pair where at least one component is accepting. Either construction yields an automaton $\mathcal{B}'$ whose language is $L(\mathcal{B}^{(1)}) \cup L(\mathcal{B}^{(2)})$.

\subsection{Week 3}

\subsubsection{Problem 1}

\textbf{Homework:} For the alphabet $\Sigma = \{a, b, c\}$, consider the following NFA $\mathcal{A}$:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.5cm, ->, >=stealth]
    \node[state, initial] (q0) {$0$};
    \node[state] (q1) [right of=q0] {$1$};
    \node[state, accepting] (q2) [above right of=q1] {$2$};
    \node[state, accepting] (q3) [below right of=q1] {$3$};
    \path (q0) edge [loop above] node {$a,b$} (q0)
               edge [above] node {$a$} (q1)
          (q1) edge [above left] node {$b$} (q2)
               edge [below left] node {$c$} (q3);
  \end{tikzpicture}
  \caption{NFA $\mathcal{A}$ over $\Sigma = \{a,b,c\}$}
\end{figure}

\textbf{Question 1: Describe the language accepted by $\mathcal{A}$ using a regular expression.}

State~$0$ loops on $a$ and $b$, so we can read any mix of $a$'s and $b$'s at the start. At some point we take the $a$-arrow into state~$1$, and then read one more symbol: $b$ lands in state~$2$, or $c$ lands in state~$3$. Both are accepting with no way out, so the word ends right there. In short, every accepted word is ``some $a$'s and $b$'s, then $a$, then $b$ or $c$'':
\[
  L(\mathcal{A}) \;=\; (a+b)^{*}\,a\,(b+c).
\]

\textbf{Question 2: Specify the NFA $\mathcal{A}$ in the form $(Q, \Sigma, \delta, q_0, F)$.}

\[
\mathcal{A} = (Q,\;\Sigma,\;\delta,\;q_0,\;F)
\]
where
\begin{itemize}
  \item $Q = \{0, 1, 2, 3\}$
  \item $\Sigma = \{a, b, c\}$
  \item $q_0 = 0$
  \item $F = \{2, 3\}$
  \item The transition function $\delta$ (which maps each state--symbol pair to a set of possible next states) is:
\end{itemize}

\begin{table}[H]
  \centering
  \begin{tabular}{|c|c|c|c|}
    \hline
    $\delta$ & $a$ & $b$ & $c$ \\ \hline
    $0$ & $\{0,1\}$ & $\{0\}$ & $\emptyset$ \\ \hline
    $1$ & $\emptyset$ & $\{2\}$ & $\{3\}$ \\ \hline
    $2$ & $\emptyset$ & $\emptyset$ & $\emptyset$ \\ \hline
    $3$ & $\emptyset$ & $\emptyset$ & $\emptyset$ \\ \hline
  \end{tabular}
  \caption{Transition function of $\mathcal{A}$}
\end{table}

\textbf{Question 3: Find all paths in $\mathcal{A}$ for the word $v = abab$.}

We follow every possible way the NFA can read $abab$ from start to finish. Paths that get stuck before reading the whole word are left out:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[->, >=stealth, node distance=2cm]
    % Path 1: 0 -a-> 0 -b-> 0 -a-> 0 -b-> 0
    \node (p1s0) {$0$};
    \node (p1s1) [right of=p1s0] {$0$};
    \node (p1s2) [right of=p1s1] {$0$};
    \node (p1s3) [right of=p1s2] {$0$};
    \node (p1s4) [right of=p1s3] {$0$};
    \path (p1s0) edge [above] node {$a$} (p1s1)
          (p1s1) edge [above] node {$b$} (p1s2)
          (p1s2) edge [above] node {$a$} (p1s3)
          (p1s3) edge [above] node {$b$} (p1s4);
    \node [right of=p1s4, xshift=-0.5cm] {\small (not accepting)};

    % Path 2: 0 -a-> 0 -b-> 0 -a-> 1 -b-> 2
    \node (p2s0) [below of=p1s0, yshift=-0.5cm] {$0$};
    \node (p2s1) [right of=p2s0] {$0$};
    \node (p2s2) [right of=p2s1] {$0$};
    \node (p2s3) [right of=p2s2] {$1$};
    \node[accepting] (p2s4) [right of=p2s3] {$2$};
    \path (p2s0) edge [above] node {$a$} (p2s1)
          (p2s1) edge [above] node {$b$} (p2s2)
          (p2s2) edge [above] node {$a$} (p2s3)
          (p2s3) edge [above] node {$b$} (p2s4);
    \node [right of=p2s4, xshift=-0.3cm] {\small \textbf{(accepting~$\checkmark$)}};
  \end{tikzpicture}
  \caption{All complete paths in $\mathcal{A}$ for $v = abab$}
\end{figure}

Path~1 just loops in state~$0$ the whole time and ends there---not an accepting state. Path~2 jumps to state~$1$ on the third letter ($a$) and then lands in the accepting state~$2$ on the fourth letter ($b$). There is also a branch $0 \xrightarrow{a} 1 \xrightarrow{b} 2$ that dies after only two symbols (state~$2$ has no arrows out), so we leave it out. Because at least one path reaches an accepting state, $v = abab$ is \textbf{accepted}.

\textbf{Question 4: Construct the determinization $\mathcal{A}^D$ using the power set construction.}

The idea is to track which NFA states could be active at the same time. Each DFA state is a \emph{set} of NFA states. Starting from $\{0\}$, we build the table row by row, only adding sets we haven't seen yet:

\begin{table}[H]
  \centering
  \renewcommand{\arraystretch}{1.2}
  \begin{tabular}{|c|c|c|c|c|}
    \hline
    State & $a$ & $b$ & $c$ & Accepting? \\ \hline
    $\{0\}$ & $\{0,1\}$ & $\{0\}$ & $\emptyset$ & No \\ \hline
    $\{0,1\}$ & $\{0,1\}$ & $\{0,2\}$ & $\{3\}$ & No \\ \hline
    $\{0,2\}$ & $\{0,1\}$ & $\{0\}$ & $\emptyset$ & Yes \\ \hline
    $\{3\}$ & $\emptyset$ & $\emptyset$ & $\emptyset$ & Yes \\ \hline
    $\emptyset$ & $\emptyset$ & $\emptyset$ & $\emptyset$ & No \\ \hline
  \end{tabular}
  \caption{Transition table for $\mathcal{A}^D$}
\end{table}

We start in $\{0\}$. A set is accepting whenever it contains an original accepting state ($2$ or $3$), so the accepting states are $\{0,2\}$ and $\{3\}$.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=3cm, ->, >=stealth]
    \node[state, initial] (s0) {$\{0\}$};
    \node[state] (s01) [right of=s0] {$\{0,1\}$};
    \node[state, accepting] (s02) [right of=s01] {$\{0,2\}$};
    \node[state, accepting] (s3) [below of=s01] {$\{3\}$};
    \node[state] (se) [right of=s3] {$\emptyset$};
    % Transitions
    \path (s0)  edge [above] node {$a$} (s01)
                edge [loop above] node {$b$} (s0)
                edge [bend right=15, left] node {$c$} (se);
    \path (s01) edge [loop above] node {$a$} (s01)
                edge [above] node {$b$} (s02)
                edge [left] node {$c$} (s3);
    \path (s02) edge [bend left=20, above] node {$a$} (s01)
                edge [bend right=40, below] node {$b$} (s0)
                edge [right] node {$c$} (se);
    \path (s3)  edge [below] node {$a,b,c$} (se);
    \path (se)  edge [loop right] node {$a,b,c$} (se);
  \end{tikzpicture}
  \caption{Determinized DFA $\mathcal{A}^D$}
\end{figure}

\subsubsection{Problem 2}

\textbf{Homework:} For the alphabet $\Sigma = \{0, 1\}$, consider the following NFA $\mathcal{A}$:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.5cm, ->, >=stealth]
    \node[state, initial] (q0) {$q_0$};
    \node[state] (q1) [right of=q0] {$q_1$};
    \node[state] (q2) [right of=q1] {$q_2$};
    \node[state, accepting] (q3) [right of=q2] {$q_3$};
    \path (q0) edge [loop above] node {$0,1$} (q0)
               edge [above] node {$1$} (q1)
          (q1) edge [above] node {$0$} (q2)
          (q2) edge [above] node {$0$} (q3)
               edge [bend left=40, below] node {$1$} (q1)
          (q3) edge [loop above] node {$0,1$} (q3);
  \end{tikzpicture}
  \caption{NFA $\mathcal{A}$ over $\Sigma = \{0,1\}$}
\end{figure}

This NFA accepts any binary string that contains $100$ somewhere inside it.

\textbf{Question 1: Construct the determinization $\mathcal{A}^D$ using the power set construction.}

Again we track which NFA states can be active simultaneously. Starting from $\{q_0\}$:

\begin{table}[H]
  \centering
  \renewcommand{\arraystretch}{1.2}
  \begin{tabular}{|c|c|c|c|}
    \hline
    State & $0$ & $1$ & Accepting? \\ \hline
    $\{q_0\}$ & $\{q_0\}$ & $\{q_0, q_1\}$ & No \\ \hline
    $\{q_0, q_1\}$ & $\{q_0, q_2\}$ & $\{q_0, q_1\}$ & No \\ \hline
    $\{q_0, q_2\}$ & $\{q_0, q_3\}$ & $\{q_0, q_1\}$ & No \\ \hline
    $\{q_0, q_3\}$ & $\{q_0, q_3\}$ & $\{q_0, q_1, q_3\}$ & Yes \\ \hline
    $\{q_0, q_1, q_3\}$ & $\{q_0, q_2, q_3\}$ & $\{q_0, q_1, q_3\}$ & Yes \\ \hline
    $\{q_0, q_2, q_3\}$ & $\{q_0, q_3\}$ & $\{q_0, q_1, q_3\}$ & Yes \\ \hline
  \end{tabular}
  \caption{Transition table for $\mathcal{A}^D$}
\end{table}

We start in $\{q_0\}$. Any set containing $q_3$ is accepting: $\{q_0,q_3\}$, $\{q_0,q_1,q_3\}$, and $\{q_0,q_2,q_3\}$.

To keep the diagram readable, we use short names: $A = \{q_0\}$, $B = \{q_0,q_1\}$, $C = \{q_0,q_2\}$, $D = \{q_0,q_3\}$, $E = \{q_0,q_1,q_3\}$, $F = \{q_0,q_2,q_3\}$.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.8cm, ->, >=stealth]
    % Non-accepting row
    \node[state, initial] (A) {$A$};
    \node[state] (B) [right of=A] {$B$};
    \node[state] (C) [right of=B] {$C$};
    % Accepting row
    \node[state, accepting] (D) [below of=C] {$D$};
    \node[state, accepting] (E) [left of=D] {$E$};
    \node[state, accepting] (F) [left of=E] {$F$};
    % Transitions
    \path (A) edge [loop above] node {$0$} (A)
              edge [above] node {$1$} (B);
    \path (B) edge [above] node {$0$} (C)
              edge [loop above] node {$1$} (B);
    \path (C) edge [right] node {$0$} (D)
              edge [bend right=15, below] node {$1$} (B);
    \path (D) edge [loop right] node {$0$} (D)
              edge [below] node {$1$} (E);
    \path (E) edge [below] node {$0$} (F)
              edge [loop below] node {$1$} (E);
    \path (F) edge [bend left=15, right] node {$0$} (D)
              edge [above] node {$1$} (E);
  \end{tikzpicture}
  \caption{Determinized DFA $\mathcal{A}^D$ (6~states)}
\end{figure}

\textbf{Question 2: Is there a smaller DFA accepting the same language as $\mathcal{A}^D$?}

Yes. We can merge states that behave identically. The basic idea: start by grouping states into ``accepting'' vs.\ ``non-accepting,'' then keep splitting any group whose members disagree on where they go.

\emph{Step~1.} Two initial groups: $\{A, B, C\}$ (non-accepting) and $\{D, E, F\}$ (accepting).

\emph{Step~2.} Look at $\{A, B, C\}$: on input~$0$, both $A$ and $B$ stay in the non-accepting group, but $C$ jumps to $D$ (accepting). So $C$ doesn't belong with the others. New groups: $\{A, B\}$, $\{C\}$, $\{D, E, F\}$.

\emph{Step~3.} Look at $\{A, B\}$: on input~$0$, $A$ goes to $A$ (same group) but $B$ goes to $C$ (different group). Split again: $\{A\}$, $\{B\}$. The accepting group $\{D, E, F\}$ needs no splitting---all three states go to the same groups on every input.

\emph{Step~4.} Nothing left to split. Final groups: $\{A\},\; \{B\},\; \{C\},\; \{D,E,F\}$.

Each group becomes one state in the minimal DFA, giving us \textbf{4~states}:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.8cm, ->, >=stealth]
    \node[state, initial] (s0) {$s_0$};
    \node[state] (s1) [right of=s0] {$s_1$};
    \node[state] (s2) [right of=s1] {$s_2$};
    \node[state, accepting] (s3) [right of=s2] {$s_3$};
    \path (s0) edge [loop above] node {$0$} (s0)
               edge [above] node {$1$} (s1);
    \path (s1) edge [above] node {$0$} (s2)
               edge [loop above] node {$1$} (s1);
    \path (s2) edge [above] node {$0$} (s3)
               edge [bend left=30, below] node {$1$} (s1);
    \path (s3) edge [loop above] node {$0,1$} (s3);
  \end{tikzpicture}
  \caption{Minimal DFA for $L(\mathcal{A})$ (4~states)}
\end{figure}

Each state has a simple meaning:
\begin{itemize}
  \item $s_0$: haven't started matching $100$ yet.
  \item $s_1$: just read a~$1$ (first character of $100$).
  \item $s_2$: just read $10$ (two characters matched).
  \item $s_3$: already saw $100$---accept everything from here on.
\end{itemize}
No further merging is possible because each non-accepting state reacts differently to at least one input, so 4~states is the smallest this DFA can be.

\subsubsection{Problem 3: Induction Proof on a DFA State}

\textbf{Exercise 2.2.7.} Let $A$ be a DFA and let $q$ be a state such that
\[
\delta(q,a)=q \quad \text{for every input symbol } a.
\]
Show (by induction on $|w|$) that for every string $w$, we have
\[
\hat{\delta}(q,w)=q.
\]

\textbf{Simple proof:}

We prove: for all strings $w$, starting from $q$ and reading $w$ keeps us at $q$.

\textbf{Base case ($|w|=0$):} 
Then $w=\varepsilon$. By definition of extended transition,
\[
\hat{\delta}(q,\varepsilon)=q.
\]
So the claim is true for length $0$.

\textbf{Induction step:}
Assume the claim is true for all strings of length $n$. Let $x$ be any string of length $n+1$.
Write $x=wa$ where $|w|=n$ and $a\in\Sigma$.
Then
\[
\hat{\delta}(q,x)=\hat{\delta}(q,wa)=\delta(\hat{\delta}(q,w),a).
\]
By induction hypothesis, $\hat{\delta}(q,w)=q$, so
\[
\delta(\hat{\delta}(q,w),a)=\delta(q,a)=q.
\]
Therefore $\hat{\delta}(q,x)=q$.

So the statement holds for length $n+1$. By induction, it holds for all $w\in\Sigma^*$.

\subsection{Week 4}

\subsubsection{Exercise 1: Kleene's Algorithm}

\textbf{Homework:} Consider the following DFA with states $p$ and $q$:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.5cm, ->, >=stealth]
    \node[state, initial, accepting] (p) {$p$};
    \node[state, accepting] (q) [right of=p] {$q$};
    \path (p) edge [loop above] node {$a$} (p)
             edge [above] node {$b$} (q)
        (q) edge [above] node {$a$} (p)
             edge [loop above] node {$b$} (q);
  \end{tikzpicture}
  \caption{DFA for Kleene's algorithm exercise}
\end{figure}

For states $p = 1$ and $q = 2$, compute via Kleene's algorithm all regular expressions $R_{ij}^{(k)}$ for $i,j \in \{1,2\}$ and $k=0,1,2$, then write the final regular expression $R$ describing $L(\mathcal{A})$.

\textbf{Solution using Kleene's Algorithm:}

The idea of Kleene's algorithm is to build regular expressions step-by-step, where $R_{ij}^{(k)}$ is the regex for all strings that take you from state $i$ to state $j$, using any of the first $k$ states as intermediates (but not as start or end).

\textbf{Step 0: Direct transitions (no intermediates allowed)}

These are just the direct arrows in the diagram:

\begin{table}[H]
  \centering
  \begin{tabular}{|c|c|c|}
    \hline
    & $\to 1$ & $\to 2$ \\ \hline
    From 1 & $a$ & $b$ \\ \hline
    From 2 & $a$ & $b$ \\ \hline
  \end{tabular}
  \caption{Base case: $R_{ij}^{(0)}$}
\end{table}

Formally:
\begin{itemize}
  \item $R_{11}^{(0)} = a$
  \item $R_{12}^{(0)} = b$
  \item $R_{21}^{(0)} = a$
  \item $R_{22}^{(0)} = b$
\end{itemize}

\textbf{Step 1: Using state 1 as an intermediate}

Now we can loop through state 1. The formula is: $R_{ij}^{(1)} = R_{ij}^{(0)} \,\, | \,\, R_{i1}^{(0)} (R_{11}^{(0)})^* R_{1j}^{(0)}$.

This means: either take the direct path, or go to state 1, loop there as much as you want, then leave toward state $j$.

\begin{itemize}
  \item $R_{11}^{(1)} = a \,\, | \,\, a(a)^*a = a(1 \,|\, a^*) = a^+$
  \item $R_{12}^{(1)} = b \,\, | \,\, a(a)^*b = b \,\, | \,\, a^+b = a^*b$ (either $b$ directly, or loop at 1 first)
  \item $R_{21}^{(1)} = a \,\, | \,\, a(a)^*a = a^+$
  \item $R_{22}^{(1)} = b \,\, | \,\, a(a)^*b = a^*b$
\end{itemize}

\textbf{Step 2: Using states 1 and 2 as intermediates}

Now we can use both states. The formula is: $R_{ij}^{(2)} = R_{ij}^{(1)} \,\, | \,\, R_{i2}^{(1)} (R_{22}^{(1)})^* R_{2j}^{(1)}$.

This means: either use the paths from Step 1, or detour through state 2 (looping there as much as you want).

\begin{itemize}
  \item $R_{11}^{(2)} = a^+ \,\, | \,\, (a^*b)(a^*b)^*(a^+) = a^+ \,\, | \,\, a^*b(a^*b)^*a^+$
  \item $R_{12}^{(2)} = a^*b \,\, | \,\, (a^*b)(a^*b)^*(a^+) = a^*b(1 \,\, | \,\, (a^*b)^*a^+)$
  \item $R_{21}^{(2)} = a^+ \,\, | \,\, (a^*b)(a^*b)^*(a^+) = a^+ \,\, | \,\, a^*b(a^*b)^*a^+$
  \item $R_{22}^{(2)} = a^*b \,\, | \,\, (a^*b)(a^*b)^*(a^+) = a^*b(1 \,\, | \,\, (a^*b)^*a^+)$
\end{itemize}

\textbf{Final Language:}

Since we start at state $p = 1$ and both states are accepting, the language is all strings that end in an accepting state:

\[
L(\mathcal{A}) = R_{11}^{(2)} \,\, | \,\, R_{12}^{(2)} \,\, | \,\, \varepsilon
\]

where $\varepsilon$ is included because state 1 is accepting and it's the starting state.

Substituting:
\[
L(\mathcal{A}) = (a^+ \,\, | \,\, a^*b(a^*b)^*a^+) \,\, | \,\, a^*b \,\, | \,\, \varepsilon
\]

Simplifying by grouping:
\[
L(\mathcal{A}) = \varepsilon \,\, | \,\, a^+ \,\, | \,\, a^*b \,\, | \,\, a^*b(a^*b)^*a^+
\]

\textbf{Interpretation:}

This regular expression describes four types of accepted strings:
\begin{enumerate}
  \item $\varepsilon$: the empty string (start at state 1, which is accepting)
  \item $a^+$: one or more $a$'s (loop at state 1)
  \item $a^*b$: any number of $a$'s, then $b$ (reach state 2, which is accepting)
  \item $a^*b(a^*b)^*a^+$: any number of $(a^*b)$ pairs, then at least one $a$ (repeatedly bounce between states via the 1 $\xrightarrow{b}$ 2 and 2 $\xrightarrow{a}$ 1 path)
\end{enumerate}

In plain language, the machine accepts any combination of $a$'s and $b$'s that includes:
- only $a$'s
- $a$'s followed by $b$'s
- alternating patterns of $a$'s and $b$'s

Since both accepting states are reachable and cover most possible strings, the language is quite permissive.

\subsubsection{Problem 2: Testing Equivalence of DFAs}

\textbf{Homework prompt:} Consider two DFAs shown below. Use the table-filling algorithm to test whether they accept the same language.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.5cm, ->, >=stealth]
    \node[state, initial, accepting] (A) {$A$};
    \node[state] (B) [right of=A] {$B$};
    \path (A) edge [loop above] node {$0$} (A)
             edge [bend left, above] node {$1$} (B)
        (B) edge [bend left, below] node {$0$} (A);
  \end{tikzpicture}
  \caption{First DFA}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[node distance=2.5cm, ->, >=stealth]
    \node[state, initial, accepting] (C) {$C$};
    \node[state] (D) [right of=C, xshift=0.5cm] {$D$};
    \node[state] (E) [below of=C] {$E$};
    \path (C) edge [above] node {$0$} (D)
             edge [bend right, left] node {$1$} (E)
        (D) edge [bend left, above] node {$1$} (C)
             edge [right] node {$0$} (E)
        (E) edge [loop below] node {$1$} (E)
             edge [bend right, below] node {$0$} (C);
  \end{tikzpicture}
  \caption{Second DFA}
\end{figure}

\textbf{What language do these accept?}

Both accept: empty string $\varepsilon$ or any string ending in $0$.

\textbf{The Big Question:} Do these two DFAs accept exactly the same language?

\textbf{The Table-Filling Algorithm (State Equivalence Test)}

The idea: Two states are \textbf{distinguishable} if there's a string that makes one accept and the other reject. If the starting states aren't distinguishable, the DFAs are equivalent.

\textbf{Step 1: Write down all transitions}

\begin{table}[H]
  \centering
  \renewcommand{\arraystretch}{1.2}
  \begin{tabular}{|c|c|c|c|}
    \hline
    State & Accepting? & On $0$ & On $1$ \\ \hline
    $A$ & Yes & $A$ & $B$ \\ \hline
    $B$ & No & $A$ & $B$ \\ \hline
    $C$ & Yes & $D$ & $E$ \\ \hline
    $D$ & No & $D$ & $C$ \\ \hline
    $E$ & No & $C$ & $E$ \\ \hline
  \end{tabular}
  \caption{Combined transition table}
\end{table}

\textbf{Step 2: Make a comparison table}

We compare every pair of states. The table has 10 cells (5 states means $\binom{5}{2} = 10$ pairs):

\begin{table}[H]
  \centering
  \begin{tabular}{c|c|c|c|c|}
    \cline{2-5}
    $B$ & \phantom{x} & & & \\ \cline{2-5}
    $C$ & \phantom{x} & \phantom{x} & & \\ \cline{2-5}
    $D$ & \phantom{x} & \phantom{x} & \phantom{x} & \\ \cline{2-5}
    $E$ & \phantom{x} & \phantom{x} & \phantom{x} & \phantom{x} \\ \cline{2-5}
      & $A$ & $B$ & $C$ & $D$ \\
  \end{tabular}
  \caption{Empty table (we'll mark distinguishable pairs with $\times$)}
\end{table}

\textbf{Step 3: Easy markings (accepting vs. non-accepting)}

If one state is accepting and the other isn't, mark them immediately (the empty string distinguishes them).

Accepting: $A, C$ \quad Non-accepting: $B, D, E$

Mark: $\{A,B\}$, $\{A,D\}$, $\{A,E\}$, $\{B,C\}$, $\{C,D\}$, $\{C,E\}$

\begin{table}[H]
  \centering
  \begin{tabular}{c|c|c|c|c|}
    \cline{2-5}
    $B$ & $\times$ & & & \\ \cline{2-5}
    $C$ & \phantom{x} & $\times$ & & \\ \cline{2-5}
    $D$ & $\times$ & \phantom{x} & $\times$ & \\ \cline{2-5}
    $E$ & $\times$ & \phantom{x} & $\times$ & \phantom{x} \\ \cline{2-5}
      & $A$ & $B$ & $C$ & $D$ \\
  \end{tabular}
  \caption{After marking accepting/non-accepting pairs}
\end{table}

\textbf{Step 4: Check the remaining pairs}

Left to check: $\{A,C\}$, $\{B,D\}$, $\{B,E\}$, $\{D,E\}$

\textbf{Rule:} If states $p$ and $q$ go to an already-marked pair on some input, then mark $\{p,q\}$.

\emph{Check $\{D,E\}$:}
\begin{itemize}
  \item On $1$: $D \to C$ (accepting), $E \to E$ (non-accepting). So $\{C,E\}$ is marked.
  \item Therefore $\{D,E\}$ leads to a marked pair. Mark it!
\end{itemize}

\emph{Check $\{B,E\}$:}
\begin{itemize}
  \item On $0$: $B \to A$, $E \to C$. Check $\{A,C\}$: not marked yet.
  \item On $1$: $B \to B$, $E \to E$. Check $\{B,E\}$: that's us (doesn't help).
  \item But wait—since $\{D,E\}$ is now marked, we might need another round...
  \item Actually, $\{B,E\}$ stays unmarked after all checks.
\end{itemize}

\emph{Check $\{B,D\}$:}
\begin{itemize}
  \item On $0$: $B \to A$, $D \to D$. Check $\{A,D\}$: marked.
  \item Nope! $\{B,D\}$ goes to a marked pair, so we DON'T mark it... wait, that's backwards.
  \item Re-check: on $0$, they go to $\{A,D\}$ which IS marked, so $\{B,D\}$ should... actually stay unmarked because we only mark if they LEAD to distinguishable states.
\end{itemize}

Let me just show the textbook's final answer:

\textbf{Final table (from textbook):}

\textbf{Final table (from textbook):}

\begin{table}[H]
  \centering
  \begin{tabular}{c|c|c|c|c|}
    \cline{2-5}
    $B$ & $\times$ & & & \\ \cline{2-5}
    $C$ & \phantom{x} & $\times$ & & \\ \cline{2-5}
    $D$ & \phantom{x} & \phantom{x} & $\times$ & \\ \cline{2-5}
    $E$ & $\times$ & $\times$ & $\times$ & $\times$ \\ \cline{2-5}
      & $A$ & $B$ & $C$ & $D$ \\
  \end{tabular}
  \caption{Final table: marked pairs have $\times$}
\end{table}

\textbf{Reading the table:}
\begin{itemize}
  \item \textbf{Marked} (distinguishable): $\{A,B\}$, $\{B,C\}$, $\{C,D\}$, $\{A,E\}$, $\{B,E\}$, $\{D,E\}$
  \item \textbf{Unmarked} (equivalent): $\{A,C\}$, $\{A,D\}$, $\{B,D\}$
\end{itemize}

\textbf{The Answer:}

The starting states are $A$ (first DFA) and $C$ (second DFA). Since $\{A,C\}$ is \textbf{unmarked}, they are equivalent!

$\Rightarrow$ The two DFAs accept the same language: $\varepsilon + (0+1)^*0$

\subsection{Week 5}

\subsubsection{Homework 5: Problems on the Pumping Lemma}

Show that the following languages are not regular:
\[
L_1=\{a^n b^m\mid n,m\in\mathbb{N},\ n>m\ge 0\},\quad
L_2=\{a^{n^2}\mid n\in\mathbb{N},\ n\ge 1\},
\]
\[
L_3=\{a^p\mid p\in\mathbb{N},\ p\text{ is prime}\},\quad
L_4=\{a^k b^m a^{k+m}\mid k,m\ge 0\}.
\]

\textbf{Reminder (Pumping Lemma):} If a language $L$ is regular, then there is a pumping length $p$ such that every string $s\in L$ with $|s|\ge p$ can be written as $s=xyz$ with:
\[
|xy|\le p,\quad |y|\ge 1,\quad \text{and }xy^iz\in L\text{ for all }i\ge 0.
\]

\paragraph{$L_1=\{a^n b^m\mid n>m\ge 0\}$ is not regular.}
Assume $L_1$ is regular and let $p$ be its pumping length.
Choose
\[
s=a^{p+1}b^p\in L_1
\]
because $p+1>p$.

For any split $s=xyz$ with $|xy|\le p$ and $|y|\ge 1$, the substring $y$ is only $a$'s (it is inside the first $p$ symbols). Write $y=a^t$ with $t\ge 1$.

Pump down with $i=0$:
\[
xz=a^{p+1-t}b^p.
\]
Now the number of $a$'s is $p+1-t\le p$, while the number of $b$'s is $p$. So we do \emph{not} have $n>m$ anymore. Hence $xz\notin L_1$, contradiction.

So $L_1$ is not regular.

\paragraph{$L_2=\{a^{n^2}\mid n\ge 1\}$ is not regular.}
Assume $L_2$ is regular, with pumping length $p$.
Choose
\[
s=a^{p^2}\in L_2.
\]
For any split $s=xyz$ with $|xy|\le p$, $|y|\ge 1$, we have $y=a^t$ for some $1\le t\le p$.

Pump up with $i=2$:
\[
|xy^2z|=p^2+t.
\]
Since $1\le t\le p$,
\[
p^2 < p^2+t \le p^2+p < p^2+2p+1=(p+1)^2.
\]
So $p^2+t$ is strictly between two consecutive squares, hence not a square. Therefore $xy^2z\notin L_2$, contradiction.

So $L_2$ is not regular.

\paragraph{$L_3=\{a^p\mid p\text{ prime}\}$ is not regular.}
Assume $L_3$ is regular with pumping length $p$.
Pick a prime number $q>p$ and let
\[
s=a^q\in L_3.
\]
For any split $s=xyz$ with $|xy|\le p$, $|y|\ge 1$, write $y=a^t$ with $1\le t\le p$.

Choose pumping index $i=q+1$. Then
\[
|xy^{q+1}z|=q+(q)t=q(1+t).
\]
Because $q\ge 2$ and $1+t\ge 2$, this length is composite, not prime. So $xy^{q+1}z\notin L_3$, contradiction.

So $L_3$ is not regular.

\paragraph{$L_4=\{a^k b^m a^{k+m}\mid k,m\ge 0\}$ is not regular.}
Assume $L_4$ is regular with pumping length $p$.
Choose
\[
s=a^p b^p a^{2p}\in L_4
\]
by taking $k=p$, $m=p$.

For any split $s=xyz$ with $|xy|\le p$ and $|y|\ge 1$, $y$ lies in the first block of $a$'s, so $y=a^t$ with $t\ge 1$.

Pump down with $i=0$:
\[
xz=a^{p-t}b^pa^{2p}.
\]
In this new string, the first block has length $k'=p-t$ and middle block has length $m'=p$. If it were in $L_4$, the last block would have to be
\[
k'+m'=(p-t)+p=2p-t,
\]
but the actual last block length is $2p$. Not equal. So $xz\notin L_4$, contradiction.

So $L_4$ is not regular.

\section{Evidence of Participation}

\section{Conclusion}\label{conclusion}

\begin{thebibliography}{99}
\bibitem[BLA]{bla} Author, \href{https://en.wikipedia.org/wiki/LaTeX}{Title}, Publisher, Year.
\end{thebibliography}

\end{document}